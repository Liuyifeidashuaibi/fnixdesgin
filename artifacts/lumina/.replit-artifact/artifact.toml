kind = "web"
previewPath = "/"
title = "LUMINA — Timeless Elegance"
version = "1.0.0"
id = "artifacts/lumina"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25762

[services.development]
run = "pnpm --filter @workspace/lumina run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lumina", "run", "build" ]
serve = "static"
publicDir = "artifacts/lumina/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25762"
BASE_PATH = "/"
